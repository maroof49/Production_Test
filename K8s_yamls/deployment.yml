apiVersion: apps/v1
kind: Deployment 
metadata:
  name: mini-project-deployment
  labels:
    app: java
spec:
  replicas: 2
  selector:
    matchlabels:
      labels:
        app: java
  template:
    metadata:
      labels:
        app: java
    spec:
      conatainer:
        - name: java-app
          image: test/test_prod:24
          ports:
          - conatinerPort: 80
        
